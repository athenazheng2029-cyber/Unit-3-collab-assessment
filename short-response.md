# Short Response Questions

1.Add each other on the repo, then when doing the work they should merge there work together

2.to work without causing problems to the main branch

3.theyre made for codespace as one makes coding lines while the other makes a project

.4. the following commands are placed in the wrong order, the rgiht is order is 

git add .

git commit -m "save work"

git push